@model ContactUsModel
@{
    Layout = "_ColumnsOne";

    //title
    NopHtml.AddTitleParts(T("PageTitle.ContactUs").Text);
    //page class
    NopHtml.AppendPageCssClassParts("html-contact-page");
}
<div class="topic-block contact-us-msg" id="contact-thanks" style="display: none">
    <div class="page contact-page">
        <div class="page-title">
            <h1>@T("PageTitle.ContactUs")</h1>
        </div>
        <div class="page-body">
            <div class="topic-block">
                <div class="topic-block-body">
                    <span class="contact-head-bold">Thank you for your inquiry</span>
                    <br /><br />
                    We will get back with you as soon as possible.
                </div>
            </div>
        </div>
    </div>
</div>

<div class="topic-block contact-us-msg" id="contact-header">
    <div class="page contact-page">
        <div class="page-title">
            <h1>@T("PageTitle.ContactUs")</h1>
        </div>
        <div class="page-body">
            <div class="topic-block">
                <div class="topic-block-body">
                    <span class="contact-head-bold">Before submitting a question</span>, please acknowledge you have read the following FAQs.
                    <ul>
                        <li>We are located in Cypress, TX and service Houston and surrouding areas (<a href="/about-us">more info</a>)</li>
                        <li>Appointments are required to see our showroom</li>
                        <li>We do not ship out of our service area.</li>
                        <li>We do not sell products, only rental</li>
                    </ul>
                    <br /><br />
                    <a href="javascript:ShowContact()">I understand</a>
                </div>
            </div>
        </div>
    </div>
</div>
</div>

<div class="page contact-page" id="contact-form-body" style="display: none">
    <div class="page-title">
        <h1>@T("PageTitle.ContactUs")</h1>
    </div>
    <div class="page-body">
        @await Component.InvokeAsync(typeof(TopicBlockViewComponent), new { systemName = "ContactUs" })
        @await Component.InvokeAsync(typeof(WidgetViewComponent), new { widgetZone = PublicWidgetZones.ContactUsTop, additionalData = Model })
        @if (Model.SuccessfullySent)
        {
            <div class="result">
                @Model.Result
            </div>
        }
        else
        {
            <form asp-route="ContactUs" method="post">
                <div asp-validation-summary="ModelOnly" class="message-error"></div>
                <div class="fieldset">
                    <div class="form-fields">
                        <div class="inputs">
                            <label asp-for="FullName" asp-postfix=":"></label>
                            <input asp-for="FullName" placeholder="@T("ContactUs.FullName.Hint")" class="fullname" />
                            <nop-required />
                            <span asp-validation-for="FullName"></span>
                        </div>
                        <div class="inputs">
                            <label asp-for="Email" asp-postfix=":"></label>
                            <input asp-for="Email" placeholder="@T("ContactUs.Email.Hint")" class="email" />
                            <nop-required />
                            <span asp-validation-for="Email"></span>
                        </div>
                        <div class="inputs">
                            <label asp-for="Phone" asp-postfix=":"></label>
                            <input asp-for="Phone" class="phone" />
                            <nop-required />
                            <span asp-validation-for="Phone"></span>
                        </div>
                        <div class="inputs">
                            <label asp-for="EventDate" asp-postfix=":"></label>
                            <input asp-for="EventDate" class="eventDate" />
                            <nop-required />
                            <span asp-validation-for="EventDate"></span>
                        </div>
                        <div class="inputs">
                            <label asp-for="Venue" asp-postfix=":"></label>
                            <input asp-for="Venue" class="venue" />
                            <nop-required />
                            <span asp-validation-for="Venue"></span>
                        </div>
                        <div class="inputs">
                            <label asp-for="HearAboutUs" asp-postfix=":"></label>
                            <select asp-for="HearAboutUs" class="HearAboutUs">
                                <option value="">-- Select --</option>
                                <option value="Friend">Friend</option>
                                <option value="Wedding Planner">Wedding Planner</option>
                                <option value="Venue">Venue</option>
                                <option value="Google">Google</option>
                                <option value="Other">Other</option>
                            </select>
                            <nop-required />
                            <span asp-validation-for="HearAboutUs"></span>
                        </div>
                        @if (Model.SubjectEnabled)
                        {
                            <div class="inputs">
                                <label asp-for="Subject" asp-postfix=":"></label>
                                <select asp-for="Subject" class="subject">
                                    <option value="">-- Select --</option>
                                    <option value="Need a quote">Need a quote</option>
                                    <option value="Schedule Appointment">Schedule Appointment</option>
                                    <option value="General Inquiry">General Inquiry</option>
                                </select>
                                <nop-required />
                                <span asp-validation-for="Subject"></span>
                            </div>
                        }
                        <div class="inputs">
                            <label asp-for="Enquiry" asp-postfix=":"></label>
                            <textarea asp-for="Enquiry" placeholder="@T("ContactUs.Enquiry.Hint")" class="enquiry"></textarea>
                            <nop-required />
                            <span asp-validation-for="Enquiry"></span>
                        </div>
                        @if (Model.DisplayCaptcha)
                        {
                            <nop-captcha />
                        }
                    </div>
                </div>
                <div class="buttons">
                    <button type="submit" name="send-email" class="button-1 contact-us-button">@T("ContactUs.Button")</button>
                </div>
            </form>
        }
        @await Component.InvokeAsync(typeof(WidgetViewComponent), new { widgetZone = PublicWidgetZones.ContactUsBottom, additionalData = Model })
    </div>
</div>

<script>
    function ShowThankYou() {
        $("#contact-thanks").show();
        $("#contact-form-body").hide();
        $("#contact-header").hide();
    }

    function ShowContact() {
        $("#contact-thanks").hide();
        $("#contact-form-body").show();
        $("#contact-header").hide();
    }
    var fromValue = (new URL(location.href)).searchParams.get('from');
    if (fromValue != null && fromValue != "") {
        ShowThankYou();
    }

    $(function () {
        try{
            if ($(".result").first().text().indexOf("successfully sent") > 0) {
                ShowThankYou();
            }
        }catch(exception){

        }
    })
</script>

